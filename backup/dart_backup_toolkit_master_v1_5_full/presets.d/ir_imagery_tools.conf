\
# presets.d/ir_imagery_tools.conf
PRESET_NAME="IR Imagery Tools WIP"
PRESET_DESC="Main IR Imagery multi-backup reconciliation preset."

ANALYZER_CMD='scripts/find_latest_flutter_source_v1.9.2.sh \
  --paths "/run/media/peddycoartte/MasterBackup/ProjectWorkingCopyBackups" \
  --pattern "*ir_imagery_tools*" \
  --group-by relpath \
  --include-instances \
  --csv "dart_audit_ir_${TODAY}.csv" \
  --json "dart_audit_ir_${TODAY}.json" \
  --log-file "find_latest_flutter_source_ir_${TODAY}.log"'

RECON_LATEST_CMD='scripts/reconstruct_latest_tree_v1.1.1.sh \
  --csv "dart_audit_ir_${TODAY}.csv" \
  --out "latest_tree_ir_${TODAY}/" \
  --latest-only \
  --verify-checksums'

RECON_FULL_CMD='scripts/reconstruct_latest_tree_v1.1.1.sh \
  --csv "dart_audit_ir_${TODAY}.csv" \
  --out "full_history_tree_ir_${TODAY}/" \
  --verify-checksums'
